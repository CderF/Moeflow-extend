# 提案:深色模式下翻译页源图降暗

## 一句话定位

深色模式下,翻译页(给图片添加标点并附上翻译)左侧源图显示区域的图片本身偏亮、有曝光感,通过主题 CSS 固定降暗图片亮度,背景保持现状。

## 已确认需求

1. **触发条件**:仅深色模式(`html[data-mt-theme="dark"]`)下生效;浅色模式完全不受影响。
2. **目标区域**:翻译页左侧的源图浏览区(给图片添加标点并附上翻译的页面);不动其他页面、不动缩略图。
3. **只调图片,背景不动**:现状背景已是深色,无需调整。
4. **降暗幅度**:轻微,`filter: brightness(0.85)` 左右;固定值,不加设置 UI。
5. **标注层保持清晰**:你画在图片上的标点/批注层不受降暗影响,只对 `<img>` 元素本身加 `filter`。
6. **实现位置**:在 `moetran-theme.css` 新增一条 `html[data-mt-theme="dark"]` 覆盖规则。

## 明确排除

- 背景再调暗(背景已深)。
- 用户可调亮度滑杆(固定幅度)。
- 全站所有图片降暗(仅翻译页源图区)。
- 浅色模式任何调整。
- 标注层一起降暗(标注保持清晰)。
- 登录态 E2E 测试(脆弱、不稳定)。

## 待办项

| 事项 | 负责人 | 时机 |
|------|--------|------|
| 具体 CSS 选择器需对照真实页面 DOM 确认 | 开发者 | 实现时;部分类名是哈希的,用 `[class*="ImageSourceViewer"]` 类 + `.main-image img` 等兜底选择器(`content.js` 约 2288 行有参考) |
| `brightness` 初值 0.85 按手动实测微调 | 开发者 | 实测后;改单一常量即可 |

## 实现要点(供后续会话参考)

- **选择器**:优先匹配源图浏览区(`[class*="ImageSourceViewer"]` / `[class*="Viewer"]`),兜底 `.main-image img`、`.article-body img`、`.pixiv-image img`(与 `content.js` 现有选择器一致);务必**只命中 `<img>` 或图片容器本身**,避免波及标注/批注层。
- **降暗方式**:`filter: brightness(0.85)` 加在图片元素上;避免对整块区域加 `filter`,否则标注层会被一起调暗。
- **浅色模式**:规则全部挂 `html[data-mt-theme="dark"]` 前缀,浅色/系统浅色下不生效。
- **改动范围**:仅 `moetran-theme.css`(manifest 中 `document_start` 注入,随主题一起应用),无需改 JS / manifest。

## 验证方案(对齐仓库现有验证文化)

- `node -c` / CSS 静态检查:新增一个轻量脚本(仿 `tests/feishu-rows-check.mjs`)断言规则存在于 `moetran-theme.css` 且选择器/值符合约定。
- 手动实测:用户在真实翻译页深色模式下确认图片亮度、标注层可读性、浅色模式无影响。
- 不新增 Playwright E2E(进翻译页需登录态 + 具体项目,不稳定)。
