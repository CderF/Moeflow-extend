# 日文符号面板:新增 5 个可自定义快捷键位

## Problem Statement

翻译组员使用「日文符号面板」输入符号时,高频符号需要反复把鼠标从输入框移到面板点击,来回移动很麻烦。需要一个快捷键输入功能:按下组合键即把符号直接插入到翻译输入框的游标处,省去打开面板和移动鼠标。

## Solution

在「日文符号面板」中新增 5 个可自定义的「快捷键位」。每个槽位绑定一个按键组合与一个符号(符号仅限面板现有 35 个),按组合键即在当前焦点输入框的游标处执行「符号插入」。面板提供「自定义」编辑模式,用户可随时改绑任一槽位的按键组合与符号。默认开箱即用:按键 `Alt/⌥+Shift+1..5`,符号为面板前 5 个 `♥ ♡ ♪ ☆ ★`。

## User Stories

1. 作为翻译组员,我希望按 `Alt/⌥+Shift+1` 就能把符号 `♥` 插入翻译输入框,这样无需移动鼠标。
2. 作为翻译组员,我希望 5 个快捷键位默认绑定面板前 5 个符号 `♥ ♡ ♪ ☆ ★`,这样开箱即用。
3. 作为翻译组员,我希望每个快捷键位的按键组合可以自定义,这样能选择顺手且不与浏览器/输入法冲突的组合。
4. 作为翻译组员,我希望每个快捷键位的符号可以自定义(仅限面板 35 个符号),这样能把最高频的符号放到最快触达的槽位。
5. 作为翻译组员,我希望自定义符号时能直观地从面板的 35 个符号中选择,这样无需记忆符号清单。
6. 作为翻译组员,我希望录制按键时被强制要求组合至少含一个修饰键,这样不会因误录单键导致正常打字被拦截。
7. 作为翻译组员,我希望把一个按键组合分配给两个槽位时被拒绝并收到提示,这样不会出现按键歧义。
8. 作为翻译组员,我希望按下快捷键时若焦点不在可输入区域,日文符号面板自动打开并提示「请先点击翻译输入框」,这样我能明白为何没有插入。
9. 作为翻译组员,我希望配置保存后刷新页面依然生效,这样不必每次重设。
10. 作为翻译组员,我希望按到未配置的组合时页面行为不受任何影响,这样不会误触页面其他功能。
11. 作为翻译组员,我希望快捷键无需先打开日文符号面板即可触发,这样能真正省掉打开面板的步骤。
12. 作为翻译组员,我希望快捷键在标准输入框、文本域与 contentEditable 富文本输入框中都能插入符号,这样各类输入场景都可使用。
13. 作为翻译组员,我希望录制按键时按 Esc 可以取消,这样录错或改主意时不至于被困在录制态。
14. 作为开发者,我希望快捷键触发复用现有的「符号插入」行为,这样点击与快捷键行为一致、无重复实现。

## Implementation Decisions

- **复用「符号插入」**:快捷键触发复用现有 `insertSymbol` 行为,与点击符号完全一致;无焦点输入框时复用面板内提示(`showJsymHint`)。不新增插入路径。
- **单个 keydown 监听**:在 content script 增加一个 document 级 `keydown` 监听,仅在命中已配置的「快捷键位」组合时 `preventDefault()` 并触发插入;其余按键完全不干预,避免干扰正常输入与页面功能。
- **按键匹配用 `e.code` 而非 `e.key`**:避免键盘布局差异(Mac 的 `Opt+数字` 会改变 `e.key`);修饰键用 `altKey` / `ctrlKey` / `metaKey` / `shiftKey`。
- **默认键位 `Alt/⌥+Shift+1..5`**:由最初设想的 `Alt+1..5` 调整而来,以规避 Chrome Win/Linux 的 `Alt+数字`「切换标签页」加速键与 Mac 的 `Opt+数字` 特殊字符输入。
- **数据模型与持久化**:5 个槽位,每槽位 `{ 按键组合, 符号 }`;持久化到 `chrome.storage.local`(键 `mt-jsym-shortcuts`),与现有 `mt-jsym-pos` 键同风格。默认值 = 前 5 个符号 + `Alt/⌥+Shift+1..5`。
- **录制模式**:进入「自定义」编辑模式后,keydown 监听临时切换为捕获模式,记录并规范化组合(如 `Alt+Shift+1`);录制期间禁用快捷键触发,防止误插入;Esc 取消录制。
- **冲突校验**:保存时若组合已被其他槽位占用,拒绝保存并提示。
- **自定义 UI**:入口加在日文符号面板头部(现有关闭按钮旁);编辑区内逐槽位「选符号 → 录制按键」。
- **不改 manifest**:不使用 `chrome.commands`,纯 content script keydown 实现。

## Testing Decisions

- **只测外部可观察行为**:按键 → 输入框内容变化;保存配置 → 刷新后仍生效;不测内部匹配/校验实现细节。
- **单接缝:Playwright E2E**,沿用 `tests/theme.spec.mjs` 的模式(`chromium.launchPersistentContext` 加载未打包扩展 → 真实 `moetran.com` 页面 → 驱动真实键盘/DOM)。
- **覆盖用例**:
  - 焦点在翻译输入框时按 `Alt+Shift+1` → 断言插入 `♥`
  - 按未配置组合(如 `Alt+Shift+6`)→ 断言输入框无变化
  - 无焦点输入框时按组合 → 断言日文符号面板打开且提示可见
  - 编辑模式:选符号 → 录制组合 → 保存 → 刷新页面 → 断言新组合仍触发插入(行为化验证持久化)
  - 重复组合 → 断言保存被拒并提示
- **既有先例**:`tests/theme.spec.mjs`、`tests/mwiki.spec.mjs`。

## Out of Scope

- 符号自由文本输入(限定面板 35 个符号)。
- popup 设置区 / 双入口(只做面板内编辑)。
- 在 `moetran.com` 以外的站点生效。
- `chrome.commands` 浏览器级快捷键。
- 面板 35 个符号清单本身的调整。

## Further Notes

- **Mac 日文输入法(Kotoeri)下 `Opt+Shift+数字` 是否被吞需实测**:若被吞,该平台用户改绑其他组合,并在面板提示中说明。
- 本 spec 使用 `CONTEXT.md` 中的领域词汇:日文符号面板、符号插入、快捷键位。
- 前期访谈完整需求与实现要点见 `proposal.md`。
